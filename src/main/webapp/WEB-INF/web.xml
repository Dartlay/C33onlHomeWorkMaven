<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Фильтр проверки возраста -->
    <filter>
        <filter-name>AgeCheckFilter</filter-name>
        <filter-class>org.time.filter.AgeCheckFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AgeCheckFilter</filter-name>
        <url-pattern>/time/*</url-pattern>
        <url-pattern>/api/*</url-pattern>
        <url-pattern>/check-age</url-pattern>
    </filter-mapping>

    <!-- Сервлет времени -->
    <servlet>
        <servlet-name>TimeServlet</servlet-name>
        <servlet-class>org.time.controller.TimeServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>TimeServlet</servlet-name>
        <url-pattern>/time/*</url-pattern>
    </servlet-mapping>

    <!-- JAX-RS -->
    <servlet>
        <servlet-name>JerseyServlet</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>org.time.controller</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>JerseyServlet</servlet-name>
        <url-pattern>/api/*</url-pattern>
    </servlet-mapping>

    <!-- Сессии -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>
</web-app>