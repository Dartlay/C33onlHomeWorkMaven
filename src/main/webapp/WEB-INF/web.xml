<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0"
         metadata-complete="false">

  <!-- 1. Обязательный слушатель для инициализации -->
  <listener>
    <listener-class>com.example.AppInitializer</listener-class>
  </listener>

  <!-- 2. Фильтр для логирования всех запросов -->
  <filter>
    <filter-name>RequestLoggingFilter</filter-name>
    <filter-class>com.example.filter.RequestLoggingFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>RequestLoggingFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
    <dispatcher>FORWARD</dispatcher>
  </filter-mapping>

  <!-- 3. Сервлет для скачивания книг -->
  <servlet>
    <servlet-name>BookServlet</servlet-name>
    <servlet-class>com.example.servlet.BookServlet</servlet-class>
    <load-on-startup>1</load-on-startup>
  </servlet>
  <servlet-mapping>
    <servlet-name>BookServlet</servlet-name>
    <url-pattern>/book</url-pattern>
  </servlet-mapping>

  <!-- 4. Сервлет для загрузки книг -->
  <servlet>
    <servlet-name>FileUploadServlet</servlet-name>
    <servlet-class>com.example.servlet.FileUploadServlet</servlet-class>
    <multipart-config>
      <max-file-size>10485760</max-file-size> <!-- 10MB -->
      <max-request-size>20971520</max-request-size> <!-- 20MB -->
      <file-size-threshold>5242880</file-size-threshold> <!-- 5MB -->
    </multipart-config>
  </servlet>
  <servlet-mapping>
    <servlet-name>FileUploadServlet</servlet-name>
    <url-pattern>/load-book</url-pattern>
  </servlet-mapping>

  <!-- 5. Настройки сессий -->
  <session-config>
    <session-timeout>30</session-timeout>
    <cookie-config>
      <http-only>true</http-only>
      <secure>false</secure>
    </cookie-config>
  </session-config>

  <!-- 6. Стартовая страница -->
  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>
</web-app>